# Qwirkle

## Libaries

numpy==2.2.6
opencv-python==4.12.0.88

## Usage

In the 355_Lucan_Cristian directory the following files/dirs are:

1. output: Where the prediction files are
2. templates: The templates I used
3. task_1.py: Where the actual implementation is. The run(input_dir) function takes the path to all the test images as input and saves all the results in output.
    Example: run(parent_parent_parent_dir + "\\antrenare\\")

Please keep all of them in the same directory.